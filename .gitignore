# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate

# Xcode — build output
build/
DerivedData/
*.xcarchive

# Swift Package Manager
.build/
.swiftpm/

# Secrets — never commit tokens, app passwords, or session state
*.env
.env
